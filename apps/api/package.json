{
  "name": "@acme/api",
  "version": "0.0.0",
  "description": "API server",
  "main": "index.js",
  "private": true,
  "scripts": {
    "build": "mkdir -p build && cp src/server.js build/server.js",
    "start": "echo 'start' && node build/server.js"
  },
  "dependencies": {
    "@acme/runtime-dependency": "*"
  }
}
