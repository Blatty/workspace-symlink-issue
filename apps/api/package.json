{
  "name": "@acme/api",
  "version": "0.0.0",
  "description": "API server",
  "main": "api/index.js",
  "private": true,
  "scripts": {
    "build": "echo 'build'",
    "start": "echo 'start'"
  },
  "dependencies": {
    "@acme/runtime-dependency": "*"
  }
}
